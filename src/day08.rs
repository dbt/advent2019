use crate::utils::{self, Result};
use crate::computer;

pub fn part1() -> Result<String> {
    return Ok("hello".to_string());
}

#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn test_() {
    }
}
